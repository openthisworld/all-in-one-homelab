---
# ExternalSecret: dex-secrets (namespace: dex)
# Syncs GitHub OAuth creds + ArgoCD OIDC client secret from Vault → k8s Secret.
# Dex pod reads these via envFrom (see platform-services/dex/values.yaml).
#
# Required Vault secrets (write once after vault-setup):
#   just vault-put platform/dex/github
#     → fields: client_id=<GitHub OAuth App ID>
#                client_secret=<GitHub OAuth App Secret>
#   just vault-put platform/dex/argocd
#     → fields: client_secret=<random string, shared with argocd-dex-client>
#
# GitHub OAuth App: Settings → Developer settings → OAuth Apps → New OAuth App
#   Homepage URL:      http://dex.homelab.local
#   Callback URL:      http://dex.homelab.local/callback
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: dex-secrets
  namespace: dex
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: dex-secrets
    creationPolicy: Owner
  data:
    - secretKey: GITHUB_CLIENT_ID
      remoteRef:
        key: secret/homelab/platform/dex/github
        property: client_id
    - secretKey: GITHUB_CLIENT_SECRET
      remoteRef:
        key: secret/homelab/platform/dex/github
        property: client_secret
    - secretKey: ARGOCD_CLIENT_SECRET
      remoteRef:
        key: secret/homelab/platform/dex/argocd
        property: client_secret
