---
# Dex — OIDC identity provider with GitHub OAuth upstream.
# See docs/adr/0007-sso-dex-github-oauth.md and docs/adr/0009-dex-static-clients-via-secretenv.md
#
# Depends on:
#   - ExternalSecret dex-secrets in namespace dex (synced by cluster-secret-store app)
#   - Vault secrets: secret/homelab/platform/dex/{github,argocd,vault}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dex
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "5"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    # Dex Helm chart. Static OAuth2 clients live in values.yaml (secretEnv field
    # reads plaintext secrets from the dex-secrets envFrom mount).
    - repoURL: https://charts.dexidp.io
      chart: dex
      targetRevision: 0.19.1
      helm:
        releaseName: dex
        valueFiles:
          - $values/platform/platform-services/dex/values.yaml
    # Git ref for Helm valueFiles above — not applied as manifests.
    - repoURL: https://github.com/openthisworld/all-in-one-homelab.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: dex
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
